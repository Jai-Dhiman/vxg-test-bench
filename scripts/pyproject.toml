[project]
name = "vxg-analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "influxdb-client>=1.44.0",
    "scikit-learn>=1.5.0",
    "numpy>=1.26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
